# 🔧 API 端点修复 - 最关键的问题！

## 🎯 根本问题发现！

经过仔细搜索项目文档，我发现了**真正的问题**！

### ❌ 错误的端点（之前用的）
```
POST /auth/signup    ❌ 不存在！
POST /auth/signin    ❌ 不存在！
POST /auth/signout   ❌ 不存在！
```

### ✅ 正确的端点（应该用的）
```
POST /auth/sign-up   ✅ 正确！(有连字符)
POST /auth/sign-in   ✅ 正确！(有连字符)
POST /auth/sign-out  ✅ 正确！(有连字符)
POST /auth/refresh   ✅ 正确！
```

## 为什么返回 HTML 404？

因为服务器上根本**没有** `/auth/signup` 这个端点！
- 服务器返回 404 Not Found
- 返回 HTML 错误页面
- 前端代码尝试解析 HTML 为 JSON → **崩溃！**

正确的端点是用**连字符** (hyphen) 分隔的：`/auth/sign-up`

## ✅ 已修复的端点

### 文件: `lib/insforge-auth-client.ts`

#### 1️⃣ signUp() 方法
```typescript
// ❌ 之前
'/auth/signup'

// ✅ 之后
'/auth/sign-up'
```

#### 2️⃣ signIn() 方法
```typescript
// ❌ 之前
'/auth/signin'

// ✅ 之后
'/auth/sign-in'
```

#### 3️⃣ signOut() 方法
```typescript
// ❌ 之前
'/auth/signout'

// ✅ 之后
'/auth/sign-out'
```

#### 4️⃣ refreshToken() 方法
```typescript
// ✅ 已正确
'/auth/refresh'
```

## 🚀 现在应该能工作了！

端点现在使用正确的格式（带连字符）：

```typescript
POST /auth/sign-up    // 注册
POST /auth/sign-in    // 登录
POST /auth/sign-out   // 登出
POST /auth/refresh    // 刷新 token
```

## 🧪 立即测试

1. **刷新浏览器** (Ctrl+Shift+R)
2. **打开开发者工具** (F12)
3. **查看 Console** 标签
4. **尝试注册新账户**
5. **应该看到 Status: 200 或 201**

## 📝 预期的正确日志

```
[InsForge] POST /auth/sign-up
[InsForge] URL: https://zrqg6y6j.us-west.insforge.app/auth/sign-up
[InsForge] Status: 201 Created  ✅
[InsForge] Response: {"user":{"id":"...","email":"..."},"token":"..."}
```

## 💡 这个修复为什么这么关键？

连字符 vs 没有连字符的区别：
- `/auth/signup` → 404 Not Found (不存在的端点)
- `/auth/sign-up` → 200 OK (正确的端点)

简单但很容易被忽略！

---

**修复日期**: 2025-12-27
**关键发现**: 端点应该使用连字符！
**状态**: ✅ 已修复，应该能工作了
