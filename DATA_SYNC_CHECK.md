# 📊 InsForge 数据同步检查指南

## 🎯 检查数据是否已同步到 InsForge 后端

### 步骤 1: 访问 InsForge 管理后台

1. 打开浏览器
2. 访问 **InsForge 控制面板**: https://zrqg6y6j.us-west.insforge.app
3. 使用你的 InsForge 账户登录

### 步骤 2: 查看数据表

在 InsForge 控制面板中，你应该看到以下表：

#### 📋 主要数据表

| 表名 | 用途 | 预期字段 |
|------|------|---------|
| **courses** 或 **users** | 用户信息 | id, email, name, created_at |
| **accounts** | 账户数据 | id, name, balance, icon, color, created_at |
| **transactions** | 交易记录 | id, type, amount, category, account_id, date, description |

### 步骤 3: 验证数据

查看每个表中是否有以下数据：

#### ✅ 成功的指标

- [ ] **courses** 表有 1+ 条记录（用户注册信息）
- [ ] **accounts** 表有 1+ 条记录（创建的账户）
- [ ] **transactions** 表有 1+ 条记录（创建的交易）

#### ❌ 失败的指标

- [ ] 所有表都是空的
- [ ] 只有 **courses** 表有数据，但 **accounts** 和 **transactions** 是空的
- [ ] 数据记录不完整（缺少字段）

---

## 🔍 如果数据没有同步怎么办？

### 检查清单

#### 1️⃣ 确认注册/登录成功
```
检查: 应用是否进入了主界面？
```

#### 2️⃣ 查看浏览器日志
按 `F12` 打开开发者工具 → Console 标签

**应该看到的日志**：
```
[InsForge] POST /auth/sign-up
[InsForge] Status: 201
[Auth Store] Syncing user data to InsForge...
[Account Store] Account synced to InsForge: ...
[Transaction Store] Transaction synced to InsForge
```

**如果看到错误日志**：
```
[InsForge Error] API Error: 404
[Account Store] Failed to sync account:...
```

#### 3️⃣ 检查 Network 请求

在浏览器开发者工具中：
1. 打开 **Network** 标签
2. 过滤 `/api/` 或 `/auth/` 请求
3. 查看每个请求的状态码：
   - `201` 或 `200` = 成功 ✅
   - `400` = 请求格式错误
   - `401` = 认证失败
   - `404` = 端点不存在
   - `500` = 服务器错误

#### 4️⃣ 检查 API 密钥和 Base URL

文件: `lib/insforge-auth-client.ts`

```typescript
// 验证这些值是否正确
const API_KEY = 'ik_39bb1da4b36fb9faef1047c398f44bf8';
const BASE_URL = 'https://zrqg6y6j.us-west.insforge.app';
```

#### 5️⃣ 检查数据同步代码

文件: `store/useAuthStore.ts`, `store/useAccountStore.ts`, `store/useTransactionStore.ts`

验证是否有：
- [ ] `syncAccountsToInsForge()` 方法
- [ ] `syncTransactionsToInsForge()` 方法
- [ ] 登录时调用同步方法
- [ ] 创建数据时立即同步

---

## 📱 手动测试数据同步

### 测试场景 1: 注册新用户

1. 打开应用 http://localhost:8081
2. 点击 "Sign Up Now"
3. 填入邮箱和密码
4. 点击 "Create Account"
5. **预期**: 进入应用首页

**检查结果**:
```
F12 → Console 查看日志
浏览器 → Network 查看请求
InsForge 后台 → courses 表查看新用户
```

### 测试场景 2: 创建账户

1. 在主界面点击 "Add Account"（或类似按钮）
2. 填入账户信息（名称、余额等）
3. 点击保存
4. **预期**: 账户出现在列表中

**检查结果**:
```
F12 → Console 查看"Account synced"日志
InsForge 后台 → accounts 表查看新账户
```

### 测试场景 3: 创建交易

1. 点击 "Add Transaction"（或类似按钮）
2. 填入交易信息
3. 点击保存
4. **预期**: 交易出现在列表中

**检查结果**:
```
F12 → Console 查看"Transaction synced"日志
InsForge 后台 → transactions 表查看新交易
```

---

## 🚀 常见问题解答

### Q: InsForge 后台数据是空的，怎么办？

**可能原因**:
1. API 端点错误 → 检查 `/auth/sign-up` 是否应该用连字符
2. API Key 无效 → 检查 `ik_39bb1da4b36fb9faef1047c398f44bf8`
3. 同步代码未执行 → 查看浏览器日志
4. 权限不足 → 检查 API Key 是否有表的写入权限

**解决方案**:
- 查看浏览器 Console 日志
- 查看 Network 标签的请求和响应
- 检查 InsForge 后台是否有错误日志

### Q: 只有 courses 表有数据，accounts 和 transactions 是空的？

**可能原因**:
- 同步代码只在登录时运行一次
- 创建账户/交易时没有调用同步

**解决方案**:
1. 重新登录，触发同步
2. 创建新的账户/交易
3. 查看浏览器日志确认同步是否成功

### Q: InsForge 后台找不到数据表？

**可能原因**:
- 数据表名称不对（应该是 `courses`, `accounts`, `transactions`）
- InsForge 项目未创建这些表

**解决方案**:
1. 在 InsForge 后台创建表
2. 或者检查表名是否正确

---

## 📝 数据表结构参考

### courses 表（用户表）

```typescript
{
  id: string;           // UUID
  email: string;        // 用户邮箱
  name?: string;        // 用户名称
  avatar_url?: string;  // 头像
  created_at: string;   // 创建时间
  updated_at: string;   // 更新时间
}
```

### accounts 表（账户表）

```typescript
{
  id: number;           // 主键
  user_id?: string;     // 用户 ID (可选)
  name: string;         // 账户名称
  balance: number;      // 余额
  icon: string;         // 图标
  color: string;        // 颜色
  created_at: string;   // 创建时间
  updated_at: string;   // 更新时间
}
```

### transactions 表（交易表）

```typescript
{
  id: number;                   // 主键
  account_id: number;           // 账户 ID
  type: 'income' | 'expense';  // 交易类型
  amount: number;               // 金额
  category: string;             // 分类
  category_icon: string;        // 分类图标
  date: string;                 // 日期
  description?: string;         // 描述
  created_at: string;           // 创建时间
  updated_at: string;           // 更新时间
}
```

---

## 🎬 快速诊断步骤

1. ✅ 注册并登录应用
2. ✅ 打开 F12 查看浏览器日志
3. ✅ 创建一个测试账户
4. ✅ 创建一笔测试交易
5. ✅ 检查 InsForge 后台数据表
6. ✅ 对比日志和数据库中的数据

---

**检查日期**: 2025-12-27
**状态**: 帮你诊断数据同步情况
